/* CDS core annotations: Hierarchy */

namespace common;

using common::CDSTypes.elementRef;

annotation Hierarchy {
  /* annotation at view level */
  leveled : array of {
    name : String(127);
    levels : array of {
      element : elementRef;
    };
  };
};
PROCEDURE "hc.hph.install.db.procedures::WriteAuditLogDefaultConfig" ( )
   LANGUAGE SQLSCRIPT
   SQL SECURITY DEFINER
   AS
BEGIN

    UPSERT 
        "hc.hph.config.db.models::Configuration.Config" 
    VALUES
        ('chp.audit','2','A','AuditLog','Switch','true','','',CURRENT_USER,CURRENT_UTCTIMESTAMP,CURRENT_USER,CURRENT_UTCTIMESTAMP) with primary key;
END

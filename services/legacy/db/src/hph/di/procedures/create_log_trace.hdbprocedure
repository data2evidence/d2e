PROCEDURE "hc.hph.di.procedures::create_log_trace" ( 
	in _AuditLogID BIGINT,
	in _LogTraceID nvarchar(256),
	in _Status nvarchar(16),
	in _Timestamp timestamp,
	in _Location nvarchar(512),
	in _Text nvarchar(5000)
) 
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER AS
BEGIN
	insert into "hc.hph.di.model::DataIntegration.AuditLogTrace" ("AuditLogID", "LogTraceID", "Status", "Timestamp","Location","Text")
		values (:_AuditLogID, :_LogTraceID, :_Status, :_Timestamp, :_Location, :_Text);
END;

PROCEDURE "hc.hph.di.procedures::delete_diextension" ( 
	IN ExtensionId NVARCHAR(256)
) 
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER AS

BEGIN
	
	delete from "hc.hph.di.model::DataIntegration.DIExtension" WHERE "ExtensionID" = :ExtensionId;
	
END;

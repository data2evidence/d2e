PROCEDURE "hc.hph.di.statistics.db.procedures::validateAuditLogID" (
	IN AuditLogID BIGINT,
	OUT cnt INTEGER
	 ) 
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER
	AS
BEGIN
	
	SELECT COUNT(*) into cnt from "hc.hph.di.statistics.db.model::Statistics.DataIngestionLoadRecords" where "AuditLogID"=:AuditLogID;
	
END;
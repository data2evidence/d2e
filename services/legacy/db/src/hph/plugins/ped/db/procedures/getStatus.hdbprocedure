PROCEDURE "hc.hph.plugins.ped.db.procedures::getStatus" (
		IN iTaskID BIGINT, 
		OUT ORESULT TABLE(STATUS VARCHAR(255))
	) 
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER
	READS SQL DATA AS
BEGIN
	ORESULT = SELECT "STATUS" FROM "hc::TASK_EXECUTIONS" WHERE "TASK_EXECUTION_ID" = :iTaskID;
END;
PROCEDURE "hc.hph.plugins.ccdi.db.procedures::getQueueEntryStatus" ( 
	IN auditLogDetailId BIGINT,
	OUT getQueueEntryStatus NVARCHAR(16)
) 
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER 

	READS SQL DATA 
	AS
BEGIN

	SELECT "STATUS" INTO getQueueEntryStatus FROM "hc.hph.plugins.ccdi.db.models::Tasks.Queue" WHERE 
	"auditLogDetailId" = :auditLogDetailId;

END; 

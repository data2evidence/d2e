PROCEDURE "hc.hph.plugins.ccdi.db.procedures::retrieveNotedScheduleId" (
		in auditLogId	BIGINT,
		out scheduleId	BIGINT
   ) 
   LANGUAGE SQLSCRIPT
   SQL SECURITY INVOKER

   --READS SQL DATA AS
   AS   
BEGIN    

SELECT "scheduleId" INTO scheduleId
 FROM "hc.hph.plugins.ccdi.db.models::Tasks.NotedScheduleId"
 WHERE "auditLogId"=auditLogId;

END;

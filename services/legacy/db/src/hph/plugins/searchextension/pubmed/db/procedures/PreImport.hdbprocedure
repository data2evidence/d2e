procedure "hc.hph.plugins.searchextension.pubmed.db.procedures::PreImport"(
		in iv_remote_src nvarchar(1000),
		in iv_path       nvarchar(1000)
	)
	language sqlscript
	sql security definer
	as
begin

end
;

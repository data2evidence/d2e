PROCEDURE "hc.hph.plugins.vcf.db.procedures.annotation::CleanUp" ( 
    IN DWAuditID BIGINT
)
   LANGUAGE SQLSCRIPT
   SQL SECURITY INVOKER
   AS
BEGIN
   CALL "hc.hph.di.procedures::clean_logs" ( :DWAuditID );
   DELETE FROM "hc.hph.genomics.db.models::SNV.VariantAnnotations" WHERE "RunAuditID" = :DWAuditID;
END
PROCEDURE "hc.hph.core.procedures::create_extension" ( 
	in it_new "hc.hph.core.model::PluginFramework.Extension"
) 
	LANGUAGE SQLSCRIPT
	SQL SECURITY INVOKER AS
	
BEGIN

	UPSERT "hc.hph.core.model::PluginFramework.Extension"("EXTENSION_ID","EXTENSIONPOINT_ID","VERSION","PLUGIN_ID")  
	SELECT "EXTENSION_ID","EXTENSIONPOINT_ID","VERSION","PLUGIN_ID" FROM :it_new;	

END;
